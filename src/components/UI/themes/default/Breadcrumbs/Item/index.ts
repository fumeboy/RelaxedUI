import normal from './normal.less'
import {L, P} from '$u/layout/Breadcrumbs/Item'
import React from "react";

let A = {
};

let C: React.FC<PP>;

class PP extends P{
}

C  = (props) => {
    let a = normal;
    return L(a)(props)
};

export default {A, P, C}